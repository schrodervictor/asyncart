{{{ header }}}
		{{{ columnLeft }}}
		{{{ contentTop }}}
		<div class="container">
			{{#breadcrumbs}}
			{{separator}}<a href="{{href}}">{{text}}</a>
			{{/breadcrumbs}}
			<h1>{{ title }}</h1>
			<p>{{ product.base.name }}</p>
			<p>{{ product.base.price }}</p>
		</div>
		{{{ contentBottom }}}
		{{{ columnRight }}}
{{{ footer }}}